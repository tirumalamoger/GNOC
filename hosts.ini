[local]
172.31.81.55 ansible_connection='ssh' ansible_ssh_user='ec2-user' ansible_ssh_private_key_file='/root/ansible/amazon-ami.pem'

[remote]
172.31.89.52 ansible_connection='ssh' ansible_ssh_user='ec2-user' ansible_ssh_private_key_file='/root/ansible/amazon-ami.pem'

# Ansible hosts inventory - List of Behavioral Inventory Parameters

# ansible_port="22" or ansible_port="2222"

# ansible_ssh_pass="tiru"

# ansible_become_user    -> Equivalent to ansible_sudo_user or ansible_su_user, allows to set the user you become through privilege escalation

# ansible_become_pass    -> Equivalent to ansible_sudo_pass or ansible_su_pass, allows you to set the privilege escalation password (never store this variable in plain text; always use a vault. See Variables and Vaults) 
